{
  "buildCommand": "cd .. && bun run build && cd demo && bun run build",
  "outputDirectory": "dist",
  "installCommand": "cd .. && bun install",
  "framework": "vite",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
